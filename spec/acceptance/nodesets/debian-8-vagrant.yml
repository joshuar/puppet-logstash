HOSTS:
  debian-8-x64:
    platform: debian-8-amd64
    box: elastic/debian-8-x86_64
    hypervisor: vagrant
CONFIG:
  type: foss
  :trace_limit: 100  # Get more than 10 lines of trace when something fails.
